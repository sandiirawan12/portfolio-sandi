import React from "react";
import HeroContent from "../../sub/uiux-design/HeroContent";

const Hero = () => {
    return (
        <div className="relative flex flex-col h-full w-full" id="about-me">
            <HeroContent />
        </div>
    );
};

export default Hero;
